export interface Cliente {
    id: number;          // ID do cliente
    nome: string;        // Nome completo
    endereco: string;        // Nome completo
    telefone: string;    // Telefone
  }
  